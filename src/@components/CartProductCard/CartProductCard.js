import React from "react";

const CartProductCard = ({
  productId,
  productTitle,
  productPrice,
  productDescription,
  productImage,
  productCategory,
}) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default CartProductCard;
